# Backlog

Structured V1 work pool. `docs/SPRINT.md` is the active execution queue.
Backlog is a user-owned optional parking lot, not a default protocol input.

## Open items

- `B-2919`: Open risk: `apps/web/src/components/InviteAcceptForm.tsx` and `apps/web/src/components/LegalAcceptanceGate.tsx` still use the old hero contract, but that is outside the strict login-screen ask unless a wider auth-entry sweep is requested.
