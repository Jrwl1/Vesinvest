# Sprint (next 12 weeks)

Window: 2026-02-10 to 2026-05-05

Exactly 5 executable DO items. Execute top-to-bottom.

| ID | Do | Files | Acceptance | Evidence | Stop | Status |
|---|---|---|---|---|---|---|
| S-01 | Implement VAT-free V1 calculations end-to-end and produce VAT-free outputs.<br>- API: remove VAT usage from budget/projection compute paths.<br>- DB: keep existing schema unchanged; do not introduce VAT dependency for V1 flows.<br>- UI: ensure V1 calculation screens do not require VAT inputs.<br>- Tests: add/update automated checks proving VAT-free outputs.<br>- Docs: align sprint evidence notes with VAT-free acceptance. | `apps/api/src/projections/**`, `apps/api/src/budgets/**`, `apps/web/src/pages/**`, related tests | No VAT value is used in V1 calculations; outputs remain VAT-free. | Test output + changed file paths in commit. | Stop if any required VAT use is mandated by a signed customer requirement; add blocker + `B-TBD` in backlog. | TODO |
| S-02 | Implement annual base-fee handling as yearly total plus yearly percent change/override.<br>- API: enforce annual base-fee input and yearly adjustment behavior.<br>- DB: reuse current persistence path; no tariff-table schema expansion.<br>- UI: expose yearly base-fee total and yearly override controls.<br>- Tests: verify yearly adjustment propagation across planning years.<br>- Docs: align evidence to ADR-013 behavior. | `apps/api/src/budgets/**`, `apps/api/src/projections/**`, `apps/web/src/pages/BudgetPage*`, related tests | Base fee can be set annually and adjusted yearly; behavior matches ADR-013. | UI/API evidence + tests + commit hash. | Stop if change requires new tariff-table scope; log blocker and stop. | TODO |
| S-03 | Implement depreciation split outputs as baseline plus investment-driven additional component.<br>- API: compute and expose both depreciation components separately.<br>- DB: persist only what is required by existing approved model; no unapproved schema scope changes.<br>- UI: render both components distinctly in projection views.<br>- Tests: verify component totals and combined total consistency.<br>- Docs: record evidence showing baseline vs investment-driven split. | `apps/api/src/projections/**`, `apps/web/src/pages/ProjectionPage*`, export/report files, related tests | Projection outputs show both depreciation components separately and consistently. | Snapshot/report sample + tests + commit hash. | Stop if split cannot be represented without schema change not approved in sprint scope. | TODO |
| S-04 | Implement V1 PDF cashflow report generation with diagram and compact table (multi-page allowed).<br>- API: generate PDF artifact from approved cashflow payload.<br>- UI: trigger export and expose generated artifact to user flow.<br>- PDF layout: include cashflow diagram and compact decision table.<br>- Tests: add regression checks for artifact generation and key sections.<br>- Docs: attach artifact evidence against acceptance checklist. | `apps/api/src/projections/**`, export utilities, `apps/web/src/pages/ProjectionPage*`, related tests | PDF answers pricing coverage question and follows approved format rules. | Generated PDF artifact + acceptance checklist + commit hash. | Stop if requested output exceeds locked V1 scope; log as backlog item. | TODO |
| S-05 | Implement hosted single-tenant release gates with build-time checks and final pre-release security audit checks.<br>- Build gate: enforce required checks before release candidate promotion.<br>- Security gate: enforce pre-release audit checklist completion.<br>- Ops gate: ensure hosted single-tenant readiness checklist is complete.<br>- Tests/checks: verify gates fail when required evidence is missing.<br>- Docs: capture run evidence and release decision record. | CI/deploy docs and configs, `docs/DEPLOYMENT.md`, relevant pipeline files | Release gate checklist is executable and blocks release when unmet. | Gate run evidence + checklist + commit hash. | Stop if platform prerequisites are missing; add blocker with owner. | TODO |
