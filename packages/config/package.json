{
  "name": "config",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^2.0.0"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "prettier": "prettier --write ."
  }
}